This imageset expects the following files to be added:
- settings-filters.png (1x resolution)
- settings-filters@2x.png (2x resolution)

These should be screenshots showing:
System Settings → Network → Filters
with the Cutline DNS row shown as "Enabled"

This is displayed to users on macOS when they need to manually enable
the DNS settings after the app saves the configuration.
